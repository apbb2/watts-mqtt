services:
  watts-mqtt:
    build: .
    container_name: watts-mqtt
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - WAHA_TOKENS_PATH=/data/tokens.json
    volumes:
      - ./data:/data
